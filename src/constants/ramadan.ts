/**
 * 🌙 Global Ramadan Theme Toggle
 * Set IS_RAMADAN_ACTIVE to `true` to enable the Ramadan vibes overlay site-wide.
 * Set to `false` to disable it completely — no other files need to change.
 */
export const IS_RAMADAN_ACTIVE = false;
